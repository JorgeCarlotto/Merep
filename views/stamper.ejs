<%- include('layouts/header') %> 

<!-- FORMULARIO PARA CARGAR IMAGEN -->
<form method="post" action="/stamper" enctype="multipart/form-data">

    <!-- <div class="form-group">
        <label for="my-input">Nombre</label>
        <input id="my-input" class="form-control" type="text" name="nombreImg">
    </div> -->

    <div class="form-group">
        <label for="my-input">Imagen</label>
        <input id="my-input" class="form-control-file" type="file" name="imagenes">
    </div>
    
    <button class="btn btn-primary" type="submit">Guardar</button>
</form>
<!-- FIN  FORMULARIO PARA CARGAR IMAGEN -->
<BR>
<BR>
<!-- iNICIO TABLA CON DATOS IMAGEN -->
    <table class="table table-light">
        <thead class="thead-light">
            <tr>
                <!-- <th>#</th>
                <th>Nombre</th> -->
                <th>Imagen</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
           
            <% for( let i = 0; i < stamper.length; i++ ) { %>
                <tr>

                    <td>
                      <img class="img-thumbnail" width="100" src="/img/<%=stamper[i].imagen %>" alt="">
                      
                    </td>
                    <td>
                      <div class="btn-group-vertical" role="group" aria-label="Vertical button group">

                        <form class="form-inline" method="post" action="/stamper/eliminar/<%=stamper[i].id %>" enctype="application/json">
                          
                          <button class="btn btn-danger" type="submit">Borrar</button>
      
                        </form>
                        
                      </div>
      

                    </td>
                </tr>
    
            <% } %>
        </tbody>
    </table>

    <!-- FIN TABLA CON DATOS IMAGEN -->
  

      <a class="btn btn-success" href="http://localhost:3001/stamperFinal" role="button">ESTAMPAR IMAGENES</a>
<%- include('layouts/footer') %> 